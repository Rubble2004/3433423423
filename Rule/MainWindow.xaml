<Window x:Class="Rule.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Rule"
        mc:Ignorable="d"
        Title="Rule" MinHeight="450" MinWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Border Background="Silver" Grid.ColumnSpan="2"/>
        <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2">
            <Image Source="/Res/logotip.jpg" Stretch="Fill" HorizontalAlignment="Right" MaxHeight="130" MaxWidth="130"/>
            <TextBlock
                   Margin="100 0 100 0"
                   FontSize="32"  
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontFamily="Arial Black">
            Руль - магазина автотоваров</TextBlock>

            <Button Click="Admin" Height="30" Width="113" VerticalAlignment="Bottom">Войти</Button>
        </StackPanel>
        
        <Frame Grid.Row="1" Name="MainFrame"></Frame>
    </Grid>
</Window>
